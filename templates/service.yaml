apiVersion: v1
kind: Service
metadata:
  name: {{ include "microarch-chart.fullname" . }}
  labels:
    {{- include "microarch-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: web
  selector:
    {{- include "microarch-chart.selectorLabels" . | nindent 4 }}
